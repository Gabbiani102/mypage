<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_livestock_cut_list" model="ir.ui.view">
        <field name="name">livestock.cut.list</field>
        <field name="model">livestock.cut</field>
        <field name="arch" type="xml">
            <list>
                <field name="species_id"/>
                <field name="sequence" widget="handle"/>
                <field name="code"/>
                <field name="name"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_livestock_cut_form" model="ir.ui.view">
        <field name="name">livestock.cut.form</field>
        <field name="model">livestock.cut</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="species_id"/>
                        <field name="code"/>
                        <field name="name"/>
                        <field name="sequence"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_livestock_cut" model="ir.actions.act_window">
        <field name="name">부위</field>
        <field name="res_model">livestock.cut</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
